extends ../layout.jade
block contenido
    div(class="col-md-5 center-block no-float top-space text-left")
        h1 Editar Producto
        form(method="POST" action="/menu/#{product.id}?_method=put" style="margin-top:1.5em;" enctype="multipart/form-data")
            .form-group
                label Nombre
                input(type="text" name="title" placeholder="Nombre del producto" class="form-control" value="#{product.title}")
            .form-group
                label Deescripción
                textarea(name="description" placeholder="Descripción del producto" class="form-control")
                    =product.description
            .form-group
                label Precio
                input(type="number" name="price" placeholder="Precio del producto" class="form-control" value="#{product.price}")
            .form-group
                label Imagen del Producto
                input(type="file" name="image" class="form-control")
            .form-group
                label Contraseña
                input(type="password" name="password" placeholder="Contraeña" class="form-control")
            .form-group.text-right
                input(type="submit" class="btn btn-info" value="Guardar")